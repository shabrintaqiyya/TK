package apap.tugas.akhir.RumahSehat.service;

import java.util.List;

public interface AppointmentRestService {
    // AppointmentDTO createAppointment(AppointmentDTO appointment);
    // List<AppointmentDTO> retrieveListAppointment();
    // AppointmentDTO getAppointmentByKode(String kode);
}
